# Optional extras (not installed in slim Docker image by default)
# pip install -r requirements-optional.txt
httpx>=0.27.0
pinecone-client>=3.0.0
supabase>=2.0.0
sentence-transformers>=2.2.0
qdrant-client>=1.9.0
